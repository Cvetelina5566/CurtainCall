services:
  docs:
    image: curtaincall_local_docs
    container_name: curtaincall_local_docs
    build:
      context: .
      dockerfile: ./compose/local/docs/Dockerfile
    env_file:
      - ./.envs/.local/.django
    volumes:
      - /app/.venv
      - ./docs:/docs:z
      - ./config:/app/config:z
      - ./curtaincall:/app/curtaincall:z
    ports:
      - '9000:9000'
    command: /start-docs
